# Bootons

## Setup :

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y python3 python3-pip git python3-flask

git clone https://github.com/Edouard-Moret/Bootons.git

export FLASK_APP=app.py

flask run --host=